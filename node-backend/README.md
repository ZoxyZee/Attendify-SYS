# Attendify JS Backend

Primary local backend for Attendify.

## Start

```powershell
cd D:\attendify\Attendify\node-backend
npm install
npm run dev
```

Health check:

```text
http://127.0.0.1:5000/health
```

The Python backend remains in `python-backend` as a backup/reference implementation.

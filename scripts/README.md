# Scripts

Local helper scripts.

- `serve_spa.py`: simple static single-page-app server helper

Project services are normally started with:

```powershell
npm run start:all
```

Available scripts:

```powershell
npm run start:all
npm run start:all:clear
npm run stop:all
```

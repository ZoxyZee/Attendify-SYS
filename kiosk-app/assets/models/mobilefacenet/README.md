# MobileFaceNet Model

TensorFlow.js model files used by the kiosk app for local face embedding generation.

- `model.json`: model graph and metadata
- `group1-shard1of1.bin`: model weights

Do not rename these files unless `embeddingService.js` is updated at the same time.


# Mobile Models

On-device machine-learning model assets for the kiosk app.

- `mobilefacenet/`: TensorFlow.js model used for local face embeddings

These files are bundled into the Expo app and used by `kiosk-app/src/services/embeddingService.js`.


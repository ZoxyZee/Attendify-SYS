# Mobile Shims

Compatibility files for packages that expect Node or native modules not available in the Expo runtime.

- `react-native-fs.js`: shim for file-system imports used by recognition dependencies


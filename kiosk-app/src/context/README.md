# Mobile Context

Kiosk state and session orchestration.

- `KioskContext.jsx`: settings, auth token, company session, employee cache, logs, sync, device registration, employee enrollment

This is the main bridge between the mobile app, local SQLite, and the backend API.


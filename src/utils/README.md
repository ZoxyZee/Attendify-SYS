# Web Utils

Small browser-side helper modules.

- `attendanceExport.js`: attendance CSV/export helpers

Keep shared utility functions here when they do not belong to a page, component, or API service.


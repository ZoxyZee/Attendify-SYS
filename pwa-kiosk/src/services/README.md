# Services

Backend API wrappers for the existing Attendify backend.

- `apiClient.js`: common JSON request helper, login, employee fetch, and attendance marking.

Attendance uses `POST /attendance/mark-web` for speed and dashboard compatibility.

# Config

Small runtime constants used by the PWA kiosk.

- `api.js`: resolves the backend URL from `VITE_API_URL` or the current host on port `5000`.
- `storage.js`: localStorage keys for token, user, and device ID.
